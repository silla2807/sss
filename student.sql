CREATE TABLE STUDENT (
    id varchar (9),
    username varchar(255),
    email varchar(255),
    phone_number BIGINT(10),
    branch varchar(255),
    password varchar(255),
    PRIMARY KEY(id)
    
    );
